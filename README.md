## Dependency: libGL.so.1

If you encounter an error related to `libGL.so.1` when running the project, you may need to install the Mesa OpenGL library. You can do this on Fedora with the following command:

```bash
sudo dnf install mesa-libGL
